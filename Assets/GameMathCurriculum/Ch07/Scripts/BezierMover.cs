using UnityEngine;

public class BezierMover : MonoBehaviour
{
    Vector3 p0, p1, p2, p3;
    float duration;
    float t;

    public void Init(Vector3 start, Vector3 cp1, Vector3 cp2, Vector3 end, float time)
    {
        // 값 저장
    }

    void Update()
    {
        // t 증가
        // 베지어 계산
        // 위치 이동
        // 도착 시 Destroy
    }
}
